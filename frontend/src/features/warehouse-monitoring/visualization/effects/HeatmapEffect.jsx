import { memo } from 'react'
import { useWarehouse3D } from '../../hooks/useWarehouse3DStore'

export const HeatmapEffect = memo(function HeatmapEffect() {
  // Heatmap effect is applied within WarehouseBuilding components
  // This component exists as a logical placeholder for future enhancements
  return null
})
